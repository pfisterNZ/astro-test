---
import WebGLBackground from './WebGLBackground.astro';
---

<div id="contacto" class="relative min-h-screen w-full overflow-hidden">
  <WebGLBackground />
  
  <div class="relative z-10 w-full min-h-screen flex items-center justify-center px-4 py-20">
    <div class="w-full max-w-2xl bg-black/70 backdrop-blur-xl border border-white/20 rounded-4xl p-8 shadow-2xl">
      <div class="text-center mb-8">
        <h1 class="text-white text-4xl md:text-5xl font-bold mb-4">Contactanos</h1>
        <p class="text-gray-300 text-lg">¿Listo para transformar tu marca? Dejanos tu mensaje.</p>
      </div>

      <form class="space-y-6">
        <div class="space-y-2">
          <label for="nombre" class="block text-white text-sm font-medium">Nombre</label>
          <input 
            type="text" 
            id="nombre"
            placeholder="Tu nombre" 
            class="w-full px-4 py-3 bg-black/50 border border-white/20 rounded-xl text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-white/50 focus:border-transparent transition-all"
          />
        </div>

        <div class="space-y-2">
          <label for="email" class="block text-white text-sm font-medium">Email</label>
          <input 
            type="email" 
            id="email"
            placeholder="tu@email.com" 
            class="w-full px-4 py-3 bg-black/50 border border-white/20 rounded-xl text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-white/50 focus:border-transparent transition-all"
          />
        </div>

        <div class="space-y-2">
          <label for="mensaje" class="block text-white text-sm font-medium">Mensaje</label>
          <textarea 
            id="mensaje"
            placeholder="¿En qué podemos ayudarte?" 
            rows="4"
            class="w-full px-4 py-3 bg-black/50 border border-white/20 rounded-xl text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-white/50 focus:border-transparent transition-all resize-none"
          ></textarea>
        </div>

        <button 
          type="submit"
          class="w-full px-8 py-4 bg-white text-black font-semibold rounded-xl hover:bg-gray-100 transition-all duration-300 shadow-lg hover:shadow-xl focus:outline-none focus:ring-2 focus:ring-white/50"
        >
          Enviar Mensaje
        </button>
      </form>
    </div>
  </div>
</div>

<style>
  /* Animación de brillo en el borde */
  .border {
    position: relative;
  }
  
  .border::before {
    content: '';
    position: absolute;
    inset: -1px;
    background: linear-gradient(90deg, 
      transparent 0%, 
      transparent 0%, 
      #20202077 50%, 
      transparent 100%, 
      transparent 100%
    );
    background-size: 200% 100%;
    animation: borderShine 3s linear infinite;
    border-radius: inherit;
    z-index: -1;
  }

  @keyframes borderShine {
    0% {
      background-position: 200% 0;
    }
    100% {
      background-position: -200% 0;
    }
  }
</style>


